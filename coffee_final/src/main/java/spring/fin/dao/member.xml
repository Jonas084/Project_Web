<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="spring.fin.model.MemberDTO"> 
	
 <select id="member_idcheck" parameterType="string" resultType="member">
     SELECT * FROM member WHERE userID=#{id}
  </select>
  <select id="member_select" parameterType="string" resultType="member">
     SELECT * FROM member WHERE userID=#{id}
  </select>
  <update id="update_point" parameterType="java.util.Map">
	 update member set userPoint=#{userPoint} where userID=#{userID}
	</update>
	<update id="update_point_use" parameterType="java.util.Map">
	 update member set userPoint=userPoint-#{totalPrice} where userID=#{userID}
	</update>
	<update id="plus_postpoint" parameterType="String">
	 update member set userPoint=userPoint+10 where userID=#{writer}
	</update>
	<select id="member_point_get" parameterType="string" resultType="int">
     SELECT userPoint FROM member WHERE userID=#{id}
  </select>
  <update id="plus_coupon_point" parameterType="java.util.Map">
	 update member set userPoint=userPoint+#{userPoint} where userID=#{userID}
	</update>
	
	
</mapper>






